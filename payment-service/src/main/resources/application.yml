server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/saga_payment
    username: root
    password: example
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
